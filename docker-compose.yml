version: '3'
services:
  makara:
    image: panha_nextjs:latest
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - 3202:3000
    networks:
      - data_analytics
networks:
  data_analytics:
    external: true 
